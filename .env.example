# ============================================================
#  Telro — Root Environment File
#  Copy this to .env and fill in your values before deploying.
#  NEVER commit the real .env to version control.
# ============================================================

# ── App ─────────────────────────────────────────────────────
NODE_ENV=production
PORT=3000
LOG_LEVEL=info

# ── Security ─────────────────────────────────────────────────
# Generate with:  openssl rand -hex 64
JWT_SECRET=CHANGE_ME_64_CHARACTER_RANDOM_HEX_STRING
JWT_EXPIRE=7d

# ── Database ─────────────────────────────────────────────────
# Path inside the container (mapped to volume telro-data)
DATABASE_PATH=./data/telro.db

# ── Asterisk AMI ─────────────────────────────────────────────
# host.docker.internal resolves to host machine from inside container.
# Replace with your actual IP/hostname if running remotely.
ASTERISK_HOST=host.docker.internal
ASTERISK_PORT=5038
ASTERISK_USER=telro
ASTERISK_SECRET=CHANGE_ME_AMI_SECRET

# ── Asterisk Paths ───────────────────────────────────────────
# These paths are bind-mounted into the container (see docker-compose.yml)
ASTERISK_CONF_PATH=/etc/asterisk
ASTERISK_SOUNDS_PATH=/app/sounds
AUDIO_UPLOAD_PATH=./uploads/audio
RECORDING_PATH=./recordings
RECORDING_FORMAT=wav

# ── SIP ──────────────────────────────────────────────────────
SIP_CONTEXT=from-internal
DEFAULT_EXTENSION_CONTEXT=from-internal

# ── Billing ──────────────────────────────────────────────────
DEFAULT_RATE_PER_MINUTE=0.1
SIP_TRUNK_RATE_PER_MINUTE=0.05

# ── Optional Redis (for future scaling) ──────────────────────
# REDIS_HOST=redis
# REDIS_PORT=6379
# REDIS_DB=0
